{
  "name": "@openads/browser",
  "version": "0.1.0",
  "private": false,
  "description": "Browser runtime helpers for embedding the OpenAds advertiser tier selector.",
  "license": "MIT",
  "files": [
    "dist"
  ],
  "type": "module",
  "sideEffects": false,
  "main": "./src/index.ts",
  "module": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "publishConfig": {
    "access": "public",
    "directory": "dist"
  },
  "scripts": {
    "build": "tsdown && bun run prepare-package",
    "check-types": "tsc --noEmit --project tsconfig.json",
    "prepare-package": "bun ../../scripts/prepare-package.ts",
    "pub:release": "bun run build && npm publish ./dist --access public",
    "pub:beta": "bun run build && npm publish ./dist --access public --tag beta --no-git-checks",
    "pub:next": "bun run build && npm publish ./dist --access public --tag next --no-git-checks"
  },
  "devDependencies": {
    "@openads/tsconfig": "*",
    "typescript": "^6.0.3"
  }
}
